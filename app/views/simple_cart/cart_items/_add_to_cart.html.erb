<%= form_tag(simple_cart.cart_items_path(product.product_type, product.id),:method => 'post') do %>

   <%= number_field_tag(:quantity, 1, min: 1, class: 'form-control') %>
   <%= button_tag(t('cart.add'), class: 'btn btn-primary') %>

<% end %>